[Unit]
Description=Interactive Brokers Gateway (PAPER only) via Docker Compose
After=docker.service network-online.target
Requires=docker.service
Wants=network-online.target

[Service]
Type=oneshot
RemainAfterExit=yes
User=ec2-user
WorkingDirectory=/home/ec2-user/market_analysis/deploy/ec2/ib-gateway
Environment=HOME=/home/ec2-user
# Fail fast if credentials were never filled in.
ExecStartPre=/usr/bin/bash -c 'test -f .env && grep -q "^TWS_USERID=.\+" .env && grep -q "^TWS_PASSWORD=.\+" .env'
ExecStart=/usr/bin/bash -c 'if docker compose version >/dev/null 2>&1; then docker compose up -d; else docker-compose up -d; fi'
ExecStop=/usr/bin/bash -c 'if docker compose version >/dev/null 2>&1; then docker compose down; else docker-compose down; fi'
TimeoutStartSec=120

[Install]
WantedBy=multi-user.target
